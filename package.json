{
  "name":        "opencode-cloudflare-container",
  "version":     "1.0.0",
  "private":     true,
  "description": "OpenCode.ai container on Cloudflare",
  "main":        "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.9"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
